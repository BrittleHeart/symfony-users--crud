{{ form_start(form, {'attr': {'class': ''}}) }}
    {{ form_end(form.title, {'attr': {'class': 'form-control'}}) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
